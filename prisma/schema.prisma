// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}


datasource db {
  provider = "sqlite"
  url      = env("PRISMA_DB")
}

//scalar Json @scalar(class: "JsonScalar")

//scalar URL @experimental @db.VarChar(255)

model User {
  id        Int      @id @default(autoincrement())
  createdAt DateTime @default(now())
	pwd String?
  email     String?   @unique
  name      String?
  tstDataJSON String? // Test json conversions
  tstData2JSON String? // Test json conversions
//	role      Role     @default(USER)
  posts     Post[]
}

model Post {
  id        Int      @id @default(autoincrement())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  published Boolean  @default(false)
  title     String?   
  postJSON String? // Test json conversions
  content String?
  author    User?    @relation(fields: [authorId], references: [id])
  authorId  Int?
	categories Category[] // Many to Many
}

model Category {
  id    Int    @id @default(autoincrement())
	name String 
  posts Post[] // Many to Many
}

//enum Role {
//  USER
//  ADMIN
//}